import 'dart:typed_data';

/// Stub — never reached at runtime on a real target.
Future<void> saveFile(Uint8List bytes, String filename) {
  throw UnsupportedError('saveFile is not supported on this platform.');
}
